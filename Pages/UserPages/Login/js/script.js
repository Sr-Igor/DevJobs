availableExit()
callDarkHome()

/*call to the function that generates the 
simulated database and fills the page*/
callVacancies()

let boxlogin = document.querySelector(".box-login")
setTimeout(() => boxlogin.style.opacity = "1", 200)
